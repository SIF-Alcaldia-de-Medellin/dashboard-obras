const SkeletonSummary = ({className}) => {
    return (
        <div className={`bg-gray-500 animate-pulse min-h-[400px] overflow-auto relative rounded ${className}`}>
            
        </div>
    );
};

export default SkeletonSummary;